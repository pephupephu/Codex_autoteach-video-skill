/* === Professional Teaching PPT Styles v8 === */
.slide{position:absolute;top:0;left:0;width:1920px;height:1080px;display:none;overflow:hidden;box-sizing:border-box;background:var(--surface);color:var(--text-1)}
.slide.is-active{display:flex;flex-direction:column;z-index:10}

/* Ambient light */
.slide-atmos{position:absolute;top:0;left:0;right:0;bottom:0;pointer-events:none;overflow:hidden}
.atmos-glow{position:absolute;top:-20%;left:-10%;width:120%;height:120%;animation:pulseGlow 8s ease-in-out infinite}

/* COVER SLIDE */
.cover-slide{position:relative}
.cover-bg{position:absolute;top:0;left:0;right:0;bottom:0}
.cover-particles{position:absolute;top:0;left:0;right:0;bottom:0;overflow:hidden}
.cp{position:absolute;border-radius:50%;background:rgba(255,255,255,0.3);animation:particleFloat 6s ease-in-out infinite}
.cover-glow{position:absolute;top:0;left:0;right:0;bottom:0;z-index:1}
.cover-orb{position:absolute;border-radius:50%;animation:orbFloat 12s ease-in-out infinite;z-index:1}
.cover-content{position:relative;z-index:5;margin:auto;text-align:center;padding:60px 100px}
.cover-icon-ring{display:inline-flex;width:88px;height:88px;border-radius:50%;align-items:center;justify-content:center;background:rgba(255,255,255,0.1);backdrop-filter:blur(12px);margin-bottom:16px;border:1px solid rgba(255,255,255,0.08);animation:iconBounce 1s cubic-bezier(0.34,1.56,0.64,1) both}
.cover-icon{font-size:40px}
.cover-tag{display:inline-block;padding:6px 22px;border:1px solid rgba(255,255,255,0.15);border-radius:20px;font-size:11px;color:rgba(255,255,255,0.4);letter-spacing:3px;text-transform:uppercase;margin-bottom:20px;animation:fadeUp 0.6s ease 0.2s both}
.cover-title{font-size:60px;font-weight:800;letter-spacing:-2.5px;color:#fff;margin:0 0 16px;line-height:1.06;text-shadow:0 4px 40px rgba(0,0,0,0.12);animation:fadeUp 0.7s ease 0.3s both}
.cover-sub{font-size:20px;color:rgba(255,255,255,0.5);margin:6px auto;max-width:720px;line-height:1.6;animation:fadeUp 0.6s ease 0.4s both}
.cover-line{width:60px;height:3px;background:rgba(255,255,255,0.15);margin:24px auto;border-radius:2px;animation:scaleW 0.6s ease 0.5s both}
.cover-meta{display:flex;justify-content:center;font-size:13px;color:rgba(255,255,255,0.3);animation:fadeUp 0.6s ease 0.6s both}

/* DIVIDER SLIDE */
.divider-slide{position:relative}
.divider-bg{position:absolute;top:0;left:0;right:0;bottom:0}
.divider-overlay{position:absolute;top:0;left:0;right:0;bottom:0;background:rgba(0,0,0,0.2)}
.divider-content{margin:auto;text-align:center;position:relative;z-index:2}
.divider-ring{display:inline-flex;width:72px;height:72px;border-radius:50%;align-items:center;justify-content:center;background:rgba(255,255,255,0.08);margin-bottom:16px;animation:iconBounce 0.8s ease both}
.divider-icon{font-size:36px}
.divider-num{display:block;font-size:72px;font-weight:900;color:rgba(255,255,255,0.08);font-family:"JetBrains Mono",monospace;line-height:1;margin-bottom:8px;animation:fadeUp 0.5s ease 0.1s both}
.divider-title{font-size:48px;font-weight:700;color:#fff;margin:0;letter-spacing:-1px;animation:fadeUp 0.5s ease 0.2s both}
.divider-line{width:80px;height:3px;background:rgba(255,255,255,0.2);margin:20px auto 0;border-radius:2px;animation:scaleW 0.5s ease 0.3s both}

/* HEADER + BODY - Design A */
.hd-a{position:absolute;top:0;left:0;right:0;z-index:10}
.hd-a-inner{display:flex;align-items:center;padding:0 48px;height:48px;gap:10px;background:linear-gradient(180deg,var(--surface-2),transparent);border-bottom:1px solid var(--border);animation:headerSl 0.5s ease both}
.hd-a-icon{font-size:17px}
.hd-a-title{font-size:14px;font-weight:600;color:var(--text-2)}
.hd-a-num{margin-left:auto;font-family:"JetBrains Mono",monospace;font-size:12px;color:var(--text-3);opacity:0.5}
.body-a{padding-top:56px;flex:1;padding-left:48px;padding-right:32px;overflow-y:auto}
.content-wrap-a{animation:contFade 0.4s ease both}

/* DESIGN B - Side Panel */
.hd-b{position:absolute;top:0;left:0;right:0;height:200px;z-index:10}
.hd-b-overlay{position:absolute;top:0;left:0;right:0;bottom:0;background:rgba(0,0,0,0.15)}
.hd-b-content{position:relative;z-index:2;padding:36px 48px;display:flex;align-items:center;gap:16px}
.hd-b-icon{font-size:32px;width:56px;height:56px;border-radius:14px;background:rgba(255,255,255,0.12);display:flex;align-items:center;justify-content:center;animation:iconBounce 0.8s ease both}
.hd-b-title{font-size:32px;font-weight:700;color:#fff;margin:0;letter-spacing:-0.5px;animation:fadeUp 0.5s ease 0.1s both}
.hd-b-num{margin-left:auto;font-family:"JetBrains Mono",monospace;font-size:13px;color:rgba(255,255,255,0.4)}
.body-b{padding-top:200px;flex:1;padding-left:48px;padding-right:32px;overflow-y:auto}

/* DESIGN C - Full Width */
.hd-c{display:flex;align-items:center;padding:0 48px;height:52px;gap:10px;border-bottom:2px solid var(--accent);animation:headerSl 0.5s ease both}
.hd-c-icon{font-size:20px}
.hd-c-title{font-size:18px;font-weight:700;color:var(--text-1);letter-spacing:-0.3px}
.hd-c-num{margin-left:auto;font-family:"JetBrains Mono",monospace;font-size:13px;color:var(--text-3);opacity:0.5}
.body-c{padding:0 48px;flex:1;overflow-y:auto}
.body-c .content-wrap-c{animation:contFade 0.4s ease both}
.sec-title-c{font-size:24px;font-weight:700;margin:16px 0;padding-bottom:8px;border-bottom:2px solid var(--accent);display:inline-block}

/* CARDS */
.card-a,.card-c{display:flex;align-items:center;gap:14px;padding:13px 20px;background:var(--surface-2);border-radius:10px;font-size:19px;line-height:1.5;border:1px solid var(--border);border-left:4px solid #6366f1;box-shadow:0 2px 8px rgba(0,0,0,0.04);transition:all 0.3s;animation:cardUp 0.5s ease both}
.card-a:hover,.card-c:hover{transform:translateY(-3px);box-shadow:0 8px 24px rgba(0,0,0,0.08)}
.card-em{font-size:21px;flex-shrink:0}
.card-stack,.card-stack-c{display:flex;flex-direction:column;gap:8px;max-width:95%}
.grid-2col{display:grid;grid-template-columns:1fr 1fr;gap:10px}
.txt-a{font-size:17px;line-height:1.7;color:var(--text-2);margin:4px 0}
.tag-a{display:inline-flex;padding:4px 14px;background:var(--surface-2);border-radius:6px;margin:3px 0;font-size:13px;font-weight:600;color:var(--accent);border:1px solid var(--border)}

/* STEPS */
.steps-a{display:flex;flex-direction:column;gap:10px;max-width:90%}
.step-a{display:flex;align-items:center;gap:16px;padding:14px 18px;background:var(--surface-2);border-radius:10px;border:1px solid var(--border);border-left:3px solid var(--sc,#6366f1);animation:cardUp 0.4s ease both}
.step-num-a{flex-shrink:0;width:32px;height:32px;border-radius:8px;display:flex;align-items:center;justify-content:center;font-family:"JetBrains Mono",monospace;font-size:14px;font-weight:800;color:#fff}
.step-num-a.info{background:var(--text-3)}
.step-body-a{font-size:18px;color:var(--text-1);line-height:1.5}
.step-a.info{border-left-color:var(--text-3)}

/* STEPS FLOW C */
.steps-flow-c{display:flex;flex-direction:column;gap:8px;max-width:90%}
.step-card-c{display:flex;align-items:center;gap:16px;padding:13px 18px;background:var(--surface-2);border-radius:10px;border:1px solid var(--border);border-left:3px solid;animation:cardUp 0.4s ease both}
.step-circle{flex-shrink:0;width:30px;height:30px;border-radius:50%;color:#fff;display:flex;align-items:center;justify-content:center;font-family:"JetBrains Mono",monospace;font-size:13px;font-weight:800}
.step-text-c{font-size:17px;color:var(--text-1);line-height:1.5}

/* CHECK GRID */
.check-grid{display:grid;grid-template-columns:1fr 1fr;gap:10px;max-width:95%}
.check-card-a{display:flex;align-items:center;gap:12px;padding:12px 16px;background:var(--surface-2);border-radius:8px;font-size:16px;line-height:1.5;border:1px solid var(--border);border-left:4px solid #6366f1;animation:cardUp 0.4s ease both}
.chk{flex-shrink:0;width:22px;height:22px;border-radius:50%;background:var(--accent);color:#fff;display:flex;align-items:center;justify-content:center;font-size:12px;font-weight:700}
.chk.info{background:var(--text-3);font-style:italic}
.check-card-c{display:flex;align-items:center;gap:12px;padding:14px 18px;border-radius:10px;font-size:17px;line-height:1.5;border:1px solid;animation:cardUp 0.4s ease both}
.chk-c{flex-shrink:0;width:24px;height:24px;border-radius:50%;color:#fff;display:flex;align-items:center;justify-content:center;font-size:12px;font-weight:700}

/* GOALS */
.goal-card{display:flex;align-items:center;gap:12px;padding:15px 18px;background:var(--surface-2);border-radius:10px;font-size:17px;line-height:1.5;border:1px solid var(--border);border-left:4px solid #6366f1;box-shadow:0 2px 6px rgba(0,0,0,0.04);animation:cardUp 0.4s ease both}
.goal-em{font-size:22px;flex-shrink:0}

/* CALLOUT */
.callout-a{padding:28px 36px;background:var(--surface-2);border-radius:14px;border:2px solid var(--accent);max-width:900px;box-shadow:0 8px 32px rgba(0,0,0,0.06);animation:popIn 0.5s ease both}
.callout-ico-a{font-size:44px;margin-bottom:6px}
.callout-a h3{font-size:26px;margin:0 0 12px;color:var(--accent)}
.callout-a p{font-size:18px;line-height:1.7;margin:6px 0}
.callout-bu{font-size:17px;margin:4px 0;padding-left:12px}
.callout-c{padding:28px 36px;background:var(--surface-2);border-radius:14px;border:2px solid;max-width:900px;box-shadow:0 8px 32px rgba(0,0,0,0.06);animation:popIn 0.5s ease both}
.callout-hd{display:flex;align-items:center;gap:12px;margin-bottom:12px}
.callout-ico-c{font-size:36px}
.callout-c h3{font-size:24px;margin:0}
.callout-c p{font-size:17px;line-height:1.7;margin:6px 0}

/* CODE WINDOW */
.code-window-a{max-width:92%;animation:cardUp 0.5s ease both}
.code-bar{display:flex;align-items:center;gap:8px;padding:9px 16px;background:#151528;border-radius:10px 10px 0 0}
.cd{width:11px;height:11px;border-radius:50%}
.cd.r{background:#ff5f57}.cd.y{background:#febc2e}.cd.g{background:#28c840}
.cd-lbl{margin-left:auto;font-size:11px;color:rgba(255,255,255,0.2);font-family:"JetBrains Mono",monospace}
.code-body-a{background:#151528;color:#e4e4e7;padding:18px 22px;margin:0;font-family:"JetBrains Mono",monospace;font-size:13px;line-height:1.7;overflow-x:auto;border-radius:0 0 10px 10px}
.code-desc{font-size:16px;color:var(--text-2);margin:8px 0}
.code-line{font-size:14px;color:var(--text-3);margin:4px 0;font-family:"JetBrains Mono",monospace}
.cl-arrow{margin-right:6px}
.code-inline-a{background:#151528;border-radius:8px;padding:12px 16px;margin:6px 0;font-family:"JetBrains Mono",monospace;font-size:12px;overflow-x:auto;color:#e4e4e7}

/* TABLE */
.table-a{margin:10px 0;max-width:95%}
.table-a table{width:100%;border-collapse:separate;border-spacing:0;font-size:14px}
.table-a td{padding:9px 14px;border-bottom:1px solid var(--border);color:var(--text-2)}
.table-a .th td{background:var(--surface-2);font-weight:600;color:var(--text-1);border-top:2px solid var(--accent)}

/* ANIMATIONS */
@keyframes particleFloat{0%,100%{transform:translateY(0)scale(1);opacity:0.3}50%{transform:translateY(-25px)scale(1.3);opacity:0.6}}
@keyframes orbFloat{0%,100%{transform:translate(0,0)scale(1)}33%{transform:translate(20px,-20px)scale(1.05)}66%{transform:translate(-10px,15px)scale(0.98)}}
@keyframes pulseGlow{0%,100%{opacity:0.5;transform:scale(1)}50%{opacity:1;transform:scale(1.05)}}
@keyframes iconBounce{0%{opacity:0;transform:scale(0.3)rotate(-8deg)}50%{transform:scale(1.12)rotate(3deg)}70%{transform:scale(0.92)}100%{opacity:1;transform:scale(1)rotate(0)}}
@keyframes fadeUp{from{opacity:0;transform:translateY(20px)}to{opacity:1;transform:translateY(0)}}
@keyframes cardUp{from{opacity:0;transform:translateY(15px)scale(0.99)}to{opacity:1;transform:translateY(0)scale(1)}}
@keyframes scaleW{from{transform:scaleX(0);opacity:0}to{transform:scaleX(1);opacity:1}}
@keyframes headerSl{from{opacity:0;transform:translateY(-8px)}to{opacity:1;transform:translateY(0)}}
@keyframes contFade{from{opacity:0}to{opacity:1}}
@keyframes popIn{from{opacity:0;transform:scale(0.95)}to{opacity:1;transform:scale(1)}}

/* STAGGER ANIMATIONS */
.is-active .card-a{animation:cardUp 0.45s ease both}
.is-active .card-c{animation:cardUp 0.45s ease both}
.is-active .step-a{animation:cardUp 0.4s ease both}
.is-active .check-card-a{animation:cardUp 0.4s ease both}
.is-active .goal-card{animation:cardUp 0.4s ease both}
